<Page
    x:Class="TodayLunchUWP.Views.LunchList"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TodayLunchUWP.Views"
    xmlns:Model="using:LunchLibrary.Models"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <ListView ItemsSource="{x:Bind ViewMdoel.Addresses}">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="Model:Address">
                    <StackPanel>
                        <TextBlock Text="{x:Bind Name}"></TextBlock>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        
        <ListView ItemsSource="{x:Bind ViewMdoel.Places}">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="Model:Place">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{x:Bind Name}"></TextBlock>
                        <Button></Button>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
